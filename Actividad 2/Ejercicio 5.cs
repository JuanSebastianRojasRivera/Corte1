using System;
using System.Linq;
using System.Collections;
using System.Collections.Generic;

namespace AplicationBase
{
    class program
    {
        public static void Main()
        {
            fecha = DateTime.Now.ToString("d");
            fecha_1 = DateTime.Now.ToString("dd-MM-yyyy");
            Console.WriteLine("La fecha actual es {0}.", fecha);

            Console.WriteLine("Digite fecha con formato dd-MM-yyyy");
            fecha2 = int.Parse(Console.ReadLine());

            resultado = DateTime.
        }
    }
}
